import { SplitAndTrimPipe } from './split-and-trim-pipe';

describe('Pipe SplitAndTrim', () => {
  it('создает экземпляр', () => {
    const pipe = new SplitAndTrimPipe();
    expect(pipe).toBeTruthy();
  });
});

